# 網站專業與初學者全面優化報告

評核日期：2026-07-10  
評核角色：30 年網站設計總監、技術指標教授、香港繁中編輯、初學者可用性評審  
主要評核版本：`index.html` + `app.js` + `styles.css` 的本地靜態網站  
交叉核對：Next.js 分頁、`data/site/` 資料包、研究資料 CSV、現有驗證報告

## 1. 總結

網站已經不只是技術指標名詞表。它有 82 個指標、風險提示、交易前檢查、真實歷史 OHLCV、案例練習、交易日誌、指標比較及 TradingView 回測審核框架。就「內容範圍」和「避免把單一訊號當買賣指令」而言，方向是正確的。

但若用教授級公開教材及成熟金融教育產品的標準評核，現時仍未可以稱為 9/10 或 10/10。核心問題不是內容少，而是四個落差：

1. 資料庫結構完整，不代表公式和案例已達到可重現、可核對的教學深度。
2. 插圖大多只營造交易情境，未能準確解釋頁面內的指標或回測概念。
3. 手機版可打開，但導覽、長英文標題、表格和圖表仍有裁切或難讀情況。
4. 香港語感已比一般 AI 文案自然，但仍混入內部工程用語、英文字段和模板式句法。

**整體評分：5.8 / 10**  
教授／專業出版標準：5.7 / 10  
初學者實際使用體驗：5.9 / 10  
獨立評審發佈判斷：**REJECT，先完成 P0 及 P1 修正**

## 2. 八項評分

| 評核項目 | 教授／專業標準 | 初學者角度 | 綜合分 | 判斷 |
|---|---:|---:|---:|---|
| 1. 專業知識 | 6.0 | 6.4 | **6.2** | 範圍廣、風險意識好；公式、來源、圖表資料層次及策略證據仍未完全達標。 |
| 2. 文案深度 | 6.5 | 6.8 | **6.6** | 有訊號、錯誤和限制，但多數指標只得短摘要及一兩項限制，未形成真正的深度教材。 |
| 3. 插圖與文案相關度 | 4.8 | 5.6 | **5.2** | 情境大致相關；不少插圖只表示「這是交易網站」，未解釋該頁的具體概念。 |
| 4. 插圖細節 | 4.2 | 4.8 | **4.5** | 圖像清晰，但教學細節不足；生成圖內的圖表、文字和符號不可當作專業證據。 |
| 5. 網站 UI 設計 | 6.4 | 6.8 | **6.6** | 色彩、卡片和狀態系統一致；首屏留白、空白主圖、重複版式及資料面板優先次序仍要調整。 |
| 6. 手機閱讀舒適度 | 4.2 | 4.8 | **4.5** | 導覽、資料格、長英文標題和圖表已有可見裁切，未達發佈標準。 |
| 7. 香港語感／AI 味 | 7.0 | 7.2 | **7.1** | 「止蝕、倉位、成交量」等用語正確；仍有 `raw leads`、`support-only`、`runner-up` 等內部語言和生硬句子。 |
| 8. 初學者易理解度 | 5.7 | 5.7 | **5.7** | 有步驟、錯誤例和風險提示；但術語解釋、閱讀次序、圖表解說和實例計算仍不足。 |

### 評分尺度

- 9-10：可作公開權威教材，內容可追溯、公式可重現、圖例準確、手機及輔助科技完整通過。
- 7-8：整體成熟，但仍有可定位的內容、教學或 UI 缺口。
- 5-6：可用，但有實質專業或理解風險，需要一輪完整優化。
- 3-4：主要流程或內容可信度不足。
- 0-2：可能誤導或不能完成核心任務。

## 3. 審核證據與範圍

- 以真實瀏覽器檢查 17 個主要功能頁桌面版；記錄標題、內容量、表格、圖像、摺疊區、輸入控制、導覽狀態和橫向溢出。
- 以 375-390px 手機視窗檢查首頁、學習、工具箱、指標庫、TradingView 教學、RSI 詳情及 SuperTrend 策略詳情。
- 檢查 82 個技術指標：16 個入門、41 個中階、25 個進階。
- 檢查 10 個本地策略案例及 200 個候選策略：正式通過 0、研究用 8、拒收 2。
- 檢查 10 張生成式頁面插圖，以及指標詳情頁的真實歷史圖表配對邏輯。
- 檢查 311 筆研究材料：89 筆 high、222 筆 medium；194 筆缺年份；113 筆 book 類來源全部未填 ISBN-13。
- 視覺證據：`.omo/evidence/professor-audit-20260710/`。
- 獨立設計評審：`.omo/evidence/professor-audit-20260710-gate-review.md`。
- 香港文案／內容無障礙評審：`.omo/evidence/professor-audit-20260710-content-accessibility-gate-review.md`。
- 技術指標專業正確度評審：`.omo/evidence/professor-audit-20260710-professional-correctness-gate-review.md`。

### 3.1 初學者任務走查

| 任務 | 結果 | 主要阻礙 | 對應可用性原則 |
|---|---|---|---|
| 從首頁決定先學甚麼 | 部分通過 | 三個入口清楚，但研究資料面板很快搶走注意力。 | H8 簡潔設計、H7 使用效率 |
| 找到 RSI | 通過 | 搜尋和快捷連結有效。 | H6 辨認而非記憶 |
| 理解 RSI | 部分通過 | 圖表先出現，但公式和限制收在摺疊區；手機標籤太小。 | H2 貼近真實世界、H6 辨認 |
| 比較兩個指標 | 部分通過 | 有決策矩陣，但表格多、固定組合多，未能自由比較任意兩個。 | H7 彈性與效率 |
| 在練習場完成一次判斷 | 部分通過 | 有控制和測驗，但手機圖表難讀，操作次序仍可更明確。 | H1 系統狀態、H5 防錯 |
| 判斷一個 TradingView 策略是否可信 | 不通過 | 頁面突出 PF 和 100 目標，但正式通過數是 0，證據狀態要讀很久才明白。 | H1 狀態可見、H2 語言符合用家 |
| 在 375px 手機完成以上任務 | 不通過 | nav、資料格、長英文 H1 及圖表有裁切／需隱形橫向捲動。 | H8 簡潔、H7 效率、H5 防錯 |

### 3.2 導航與錯誤復原補充

- 無效指標 slug 會顯示「找不到這個指標」及返回按鈕，做法正確。
- 無效頂層 route 會直接回到首頁，沒有「頁面不存在」提示；讀者可能不知道網址錯誤。應加正式 404／找不到頁面狀態，列出可返回的主要入口。
- 本地資料匯入有預覽及確認，屬於良好防錯設計，應保留。

## 4. 發佈前必須處理的 P0 問題

### P0-1：TradingView「100 個案例」與實際完成度不符

**位置**：`#/strategy-cases` 首屏、審核進度面板、`data/site/tradingview_strategy_cases.json`。  
**現況**：主標寫「100 個案例先過審才展示」，但目前只有 10 個案例記錄，正式通過數是 0；其餘為 8 個 support-only、2 個 rejected。6 個有 PF，0 個有數值交易次數，0 個有數值最大回撤，10 個都缺樣本外測試，原作者 Pine 原碼數是 0。

**風險**：讀者首先看到「100 個案例」，容易以為網站已經完成 100 個真實高質素回測案例。後面雖有說明 accepted=0，但主標先建立了錯誤預期。

**詳細優化**：

1. 完成 100 個正式案例前，把 H1 改為「TradingView 策略候選庫」或「策略審核進度」。
2. 首屏直接寫「已收集 200 個候選；10 個可供研究；0 個完成全部審核」。
3. 把「100」改為目標值，不可當作現有內容數量。
4. 正式案例必須同時有：標的、週期、時區、測試期、交易次數、PF、最大回撤、成本、滑價、訂單填補假設、參數、樣本外／前向測試、代碼來源及授權狀態。
5. `support-only` 只可放在「研究候選」分頁，不可與已通過案例混排。

**完成標準**：首屏不再令人誤以為已有 100 個正式案例；每個顯示為「已通過」的案例都通過全部驗證欄位。

### P0-2：真實資料失效時，模擬圖可能仍被標成 Yahoo 快照

**位置**：`app.js:11126` 的 `marketCaseForIndicator()`、`seriesFromMarketCase()` 和 `marketCaseDisplay()`。  
**現況**：有本地資料時使用 Yahoo OHLCV；資料不存在時會轉用 `generateSeries()` 模擬走勢，但 `marketCaseDisplay()` 的 fallback 仍可顯示「Yahoo Finance chart API 本地快照」。

**風險**：一旦市場資料包缺失或格式有問題，模擬數據可能被誤標為真實資料，這是證據可信度問題，不只是 UI 問題。

**詳細優化**：

1. 市場案例函式回傳 `{ series, provenance, isSynthetic, reason }`，不可只回傳陣列。
2. `isSynthetic=true` 時，圖上必須寫「模擬數據，只作版面示範」。
3. 真實數據載入失敗時，研究型頁面應顯示資料錯誤；不要默默降級後仍沿用真實來源標籤。
4. 驗證腳本加入「synthetic series cannot carry Yahoo/local snapshot label」硬性 gate。

**完成標準**：任何模擬走勢都不會顯示真實資料來源；任何真實資料圖都可追溯到具體案例 key、下載日期及 bar 數。

### P0-3：市場寬度／情緒指標使用單一股票圖，資料層次不相符

**位置**：5 個市場寬度指標詳情頁；`app.js:11126` 把「市場寬度」和「綜合」一律配到 AAPL uptrend。  
**受影響指標**：漲跌家數線、看漲百分比指數、麥克連震盪器、新高新低數、TRIN。

**風險**：這些指標需要全市場或成份股家數／成交量資料。單一 AAPL OHLCV 不能驗證市場寬度。讀者會把「價格背景」誤當「該指標案例」。認沽認購比率等情緒指標亦需要期權資料，不能只靠個股價格圖說明。

**詳細優化**：

1. 市場寬度頁使用 NYSE／NASDAQ 或指定指數成份的 advance/decline、new high/new low、up/down volume 資料。
2. 圖表至少同時顯示指數價格與寬度線，標明 universe、成份調整及資料供應商。
3. 未取得寬度資料前，圖表標題改為「價格背景」，並明確寫「不是該指標的計算結果」。
4. 情緒指標需要期權成交量或未平倉合約資料，並分清 volume PCR 與 open-interest PCR。

**完成標準**：市場寬度／情緒圖使用正確資料層次；不再用單一股票圖代表全市場指標。

### P0-4：公式欄「有內容」但未達到可重現標準

**位置**：82 個指標資料的 `formula` 欄、指標詳情「公式、用法、錯誤和相關指標」摺疊區。  
**現況**：27/82 的公式只屬文字描述；25 個進階指標中有 13 個沒有可重現公式，例如 Ichimoku、KAMA、Fisher Transform、Mass Index、EOM、Volume Profile。

**風險**：結構驗證只檢查欄位非空，所以報告顯示 PASS；但教授或讀者仍不能按網站資料重算指標，也不知道平台版本差異。

**詳細優化**：每個指標補齊以下欄位：

| 欄位 | 內容要求 |
|---|---|
| `formulaType` | `exact`、`algorithm`、`chartMethod`，不要全部叫公式。 |
| `exactFormula` | 完整方程或逐步算法。 |
| `variables` | 每個變數及資料欄位定義。 |
| `smoothing` | SMA、EMA、Wilder RMA、WMA 等具體方法。 |
| `warmupBars` | 最少預熱 bars 及首值處理。 |
| `dataRequirements` | close、OHLC、volume、breadth、options、tick/intraday 等。 |
| `platformVariants` | TradingView、常見平台或文獻版本差異。 |
| `workedExample` | 用 5-15 筆小數據示範計算。 |
| `sourceRefs` | 原著、官方文件或高信任來源。 |

**RSI 例子**：現有公式骨架正確，但要補明平均升跌採 Wilder RMA；TradingView 官方示例亦以 `rma(gain, 14)` 和 `rma(loss, 14)` 計算。只寫「平均升幅／平均跌幅」仍不夠精確。

### P0-5：來源沒有真正接到 82 個指標，材料數亦互相矛盾

**位置**：指標研究狀態卡，`app.js:2620`；RSI 等指標的 `research.materialCount` 和 `comparisonRows[*].material_count`。  
**現況**：全部 82 個指標的 `research.materialCount` 都是 0、`topMaterials` 都是空陣列；但比較列同時列出多位作者的材料數，例如 RSI 的 Wilder 3 筆、Connors 5 筆。

**風險**：讀者不知道「0」是沒有來源、來源未接線，還是資料轉換錯誤。這會削弱整個研究庫的可信度。

**詳細優化**：

1. 統一材料數的來源欄位，不可由不同 builder 各自計算。
2. 分開顯示「研究庫材料數」和「網站已接入引用數」。
3. `0` 必須配狀態：`no_material`、`not_connected`、`filtered_out`，不可只顯示數字。
4. 驗證 gate 加入 `research.materialCount == sum/unique count represented by comparison material rows` 或明確解釋差異。

## 5. 八個評核項目的詳細分析

### 5.1 專業知識：6.2 / 10

**做得好**

- 82 個指標都有用途、參數、訊號、錯誤、限制及相關指標。
- 清楚提醒 RSI 超買不等於立即做空、回測不等於實盤、PF 不代表保證盈利。
- 有真實歷史 OHLCV、止蝕、目標、R 值、成本和滑價概念。
- TradingView 頁有審核門檻，知道要看 Strategy Report、Properties、交易次數及回撤。

**需要改善**

1. 公式精確度不足：27 個只屬描述，13 個進階指標不可重現。
2. 來源未落到 claim level：311 筆材料存在，但大多數公開段落沒有逐句來源。
3. 222/311（約 71%）材料只屬 medium；194 筆缺年份；113 筆 book 類來源缺 ISBN-13。
4. 82 個指標只分配 4 個實際在用市場案例：uptrend 供 23 個指標、reversal 供 22 個、breakout 供 33 個、range 供 4 個。
5. 圖表程式只讀 `close`，未使用已存在的 `adjClose`；SPY 樣本內 close 與 adjClose 最大差異約 4.58%，必須有企業行動／調整價政策。
6. TradingView 目前沒有一個 accepted case；不應以已完成案例庫的語氣介紹。
7. 78/82 指標少於兩項限制；72/82 少於兩項常見錯誤。失效情境未完整覆蓋市況、資料質素、延遲、流動性、參數和假訊號。
8. 全部 82 個指標的直接材料欄仍是 0；研究庫有材料，不等於網站已提供可追溯引用。

**教授級優化方向**

- 先精修 20 個核心指標至「可重算、可驗證、可教學」，再擴至 82 個。
- 每個核心指標至少有一個好例子、一個失效例、一個錯誤用法、一個計算示例、一組來源。
- 市場數據加入 adjusted/unadjusted close、拆股、派息、時區、交易時段、下載日期及覆核日期。
- 每項回測結論加 evidence tier，未有 OOS／成本／交易次數時只可顯示研究假設。

### 5.2 文案深度：6.6 / 10

**量化觀察**

- 指標摘要平均只有 36.8 個中文字元。
- 每個指標平均 2.4 個訊號、1.1 個常見錯誤、1.0 個限制。
- 72/82 個指標少於兩項常見錯誤，78/82 個少於兩項限制。
- 多個功能頁首屏可讀文字只有約 180-350 字，真正內容收在關閉的 `<details>` 內。

**問題**

- 內容範圍大，但多數指標仍是「概要卡」深度。
- `適合、常用、觀察、判斷、配合` 重複出現，句子雖不完全相同，節奏仍有資料模板感。
- 「何時有效／何時失效」未拆成市場狀態、資料條件、訊號失效及交易失效四層。
- 沒有足夠逐步推理：讀者知道結論，但未必知道怎樣由圖表得到結論。

**詳細優化**

每篇指標頁改為固定八段：

1. 一句話：這個指標回答甚麼問題。
2. 先備知識：讀者要先懂甚麼。
3. 計算：公式、變數、平滑方法、預熱期。
4. 看圖：先看哪一條線／哪個區域，再看甚麼。
5. 有效情境：趨勢、震盪、突破、事件前後。
6. 失效情境：假突破、低流動性、財報裂口、參數敏感。
7. 交易劇本：觸發、失效點、止蝕、退出、倉位。
8. 練習：一題好例、一題壞例、一題只觀察不交易。

### 5.3 插圖與文案相關度：5.2 / 10

**做得好**

- 首頁辦公桌、比較頁雙圖表、日誌頁筆記簿、TradingView 頁程式與報表，情境上容易辨認。
- 陰陽燭頁的自製 SVG 與「開、高、低、收、位置、量能、確認、R 值」直接相關，是現時最好的教學插圖。
- 指標詳情有真實歷史走勢、入場、止蝕及 2R 標記，較一般裝飾圖有價值。

**問題**

- 首頁寫「先讀圖」，但手機首屏不顯示主圖。
- 指標庫插圖是大量卡片和放大鏡，沒有展示八大用途如何分類。
- 詞彙表插圖是隨機英文字母分類卡，與技術分析詞彙關係弱。
- TradingView、策略案例、Pine Script 三個頁面重用同一張圖，難以分辨頁面目的。
- Casebook 重用 indicator-detail 圖；script-demo 重用 playground 圖，內容辨識度不足。

**詳細優化**

- 裝飾圖只負責建立氣氛；教學圖必須在圖內或旁邊回答頁面問題。
- 每張圖先定義一個 learning objective，例如「比較兩個指標是否重複」而不是「顯示兩張金融圖」。
- 所有需要文字的教學標籤用 HTML/SVG 疊加，不要把文字生成在 bitmap 內。

### 5.4 插圖細節：4.5 / 10

**問題**

- 生成圖很清晰，但圖表比例、軸、數字、代碼和卡片文字多數不可讀或沒有真實意義。
- 圖片細節是「攝影細節」，不是「教學細節」。
- 指標詳情共用同一張生成式技術圖；圖內假圖表可能與頁面下方真實資料圖互相衝突。
- 手機的真實 SVG 圖表被縮到約 375px，10-12px 的圖內標籤難以閱讀。

**詳細優化**

1. 生成圖只保留桌面、紙張、光線等情境，不顯示需要準確解讀的數據。
2. 真正教學部分用程式繪圖：座標軸、日期、價位、指標值、訊號、失效位可核對。
3. 手機圖表提供「放大查看」和文字版摘要；圖內最小字建議 14px 等效尺寸。
4. 對比圖必須使用同一標的、同一期間和同一價格基準。

### 5.5 網站 UI 設計：6.6 / 10

**做得好**

- teal／amber／red 狀態色有一致意義。
- 8px 圓角、淺色研究桌風格、按鈕和表單樣式一致。
- active nav、skip link、focus 樣式及結果狀態已有基礎。
- 資料密度高但大致整齊，適合工具型教育網站。

**主要問題**

1. 首頁桌面首屏的三張入口卡排成兩欄，第三張落到下一行，右側出現大幅無效留白。
2. 每頁都使用相近的「大卡 hero + 大卡內容」版式，久看有模板感。
3. 研究資料面板在首頁及指標庫太早出現，蓋過讀者真正想做的任務。
4. 部分 hero 圖使用 `loading="lazy"`，切換 hash route 時曾出現可見空白框；首屏主圖不應 lazy load。
5. 比較頁有大量表格；指標庫有 82 個控制／卡片，缺少更強的分組及分段導覽。
6. 表格普遍沒有 `<caption>`，`<th>` 亦沒有 `scope`，屏幕閱讀器難以理解。

**詳細優化**

- 首頁入口改為 3 欄等高或 1 個主要入口 + 2 個次要入口，與主圖同高。
- 研究狀態移到頁尾「資料來源與更新」或獨立方法論頁。
- 每個 route 保留一個主要任務，不要每區都用同一種卡片重量。
- 首屏圖用 eager/fetchpriority high；其餘圖才 lazy load，並設有穩定 aspect-ratio skeleton。
- 表格加 caption、row/col scope 和手機提示「左右滑動看完整內容」。

### 5.6 手機閱讀舒適度：4.5 / 10

**實測問題**

- 手機導覽以無 scrollbar 的橫向捲動處理；`Pine Script` 在右邊只露出一部分，畫面沒有提示可左右拖動。
- `SuperTrend AI Adaptive - Strategy BTC` 等長英文標題在窄屏出現裁切風險。
- 指標庫段落和資料卡在右側有視覺裁切；表格及長英文狀態值閱讀壓力高。
- RSI 真實圖表縮細後，日期、價位、入場／止蝕標籤難以閱讀。
- 長頁依賴多個摺疊區，但沒有手機章節導覽、閱讀進度或「回到頂部」。

**詳細優化**

1. 手機主導覽改為可展開 menu，或加入「更多」；不可只靠隱形橫向捲動。
2. 長英文標題使用 `overflow-wrap:anywhere`；金融代號可保持 no-break，普通英文不可強制不換行。
3. 所有可點擊控制最少 44x44px；chips 之間保留 8px。
4. 圖表提供全屏／放大、可橫向縮放或文字摘要。
5. 長表格前加明顯捲動提示，首欄 sticky；重要數據改成 mobile cards。
6. 指標詳情加 sticky 章節按鈕：「看圖、公式、失效、交易劇本、來源」。
7. `@media (max-width:640px)` 明確加入 `.site-data-grid { grid-template-columns:1fr; }`；手機解除會造成裁切的 `.no-break`。

### 5.7 香港語感及 AI 句子：7.1 / 10

**做得好**

- 「止蝕、倉位、成交量、裂口、派息、港股、美股」符合香港金融語境。
- 新文案如「刪掉重複訊號」、「先看位置，再看方向」簡短、有人味。
- 沒有大量「賦能、打造、一站式、全方位」等典型 AI／內地市場字眼。

**需要改善的語言類型**

1. **內部工程語言**：`raw leads 已入隊`、`accepted / support-only / rejected`、`runner-up 缺口`、`explicit status`、`taxonomy`、`momentumBreakout 保留未使用`。
2. **生硬比喻**：「先把數字降回證據」不自然，應直接寫「先核對數據來源和回測設定」。
3. **語體不一致**：同一網站同時用「唔急住」、「看」、「睇」、「資料接入」、「前端教學頁」；應選定「自然香港書面語」為主，教練提示才用少量口語。
4. **台灣／泛中文詞**：「量能」出現頻繁；香港初學者可先寫「成交量配合（量能）」或直接用「成交量」。
5. **英文縮寫未先解釋**：PF、OOS、Properties、Strategy Report、RMA、ATR、EMA 在首次出現時應有中文全名或一句解釋。

**建議語氣**

- 主要內文：自然香港書面語，短句、具體動詞、少口號。
- 提示卡：可稍口語，但避免一段內同時混用廣東話、台式術語和工程英文。
- 風險提示：直接寫發生甚麼、讀者要做甚麼，不用抽象語句。

### 5.8 初學者容易理解度：5.7 / 10

**做得好**

- 首頁提供「由零開始、查指標、做練習」三個入口。
- 多個頁面使用「先看甚麼、再看甚麼、最後看失效」的流程。
- 有錯誤案例、風險回報、成本和滑價，避免只教入場訊號。

**主要障礙**

- 初學者未理解資料庫架構，已先看到 82、72、100、311、match type、runner-up 等狀態。
- 指標頁先顯示複雜圖表，但公式、用法和錯誤收在「需要時再展開」；新手未必知道何時需要。
- PF、最大回撤、OOS、Bar Magnifier、pyramiding 等概念缺少圖解和例子。
- 指標組合以 9/10 等精確分數輸出，但未顯示權重和不確定性，容易令人把評分當真理。
- 缺少完成後的「我應該懂得甚麼」和可核對答案。

**詳細優化**

- 首次到訪只顯示三步：判斷市況、選一個工具、寫失效位。
- 每頁顯示「預計閱讀 6 分鐘／完成後你會識得……」。
- 每個新術語提供一句白話、一個數字例、一個常見誤解。
- 工具輸出由「分數」改為「覆蓋到甚麼／缺少甚麼／下一步做甚麼」。
- 每個核心頁加 3 題小測及答案理由。

## 6. 逐頁優化清單

| 位置 | 問題 | 詳細優化 | 優先級 |
|---|---|---|---|
| 全站 header | 手機 nav 隱形橫向捲動，最右項目被截。 | 改 menu／更多按鈕；顯示當前位置；44px touch target；保留 skip link。 | P1 |
| 全站 hero | 版式重複，大量白位；部分主圖 lazy load。 | 建立 3 種 hero 模式：教學、工具、資料詳情；首圖 eager；減少空白及卡中卡。 | P1 |
| 首頁 hero | 三張入口卡造成桌面大留白；手機看不到主圖。 | 主要入口置左，其餘兩個並排；手機將一張真正有教學標記的圖放在三步流程後。 | P1 |
| 首頁資料包 | 內部資料和術語太早出現。 | 首頁只留「82 個指標、5 個歷史案例、最近更新」；其餘移到方法論。 | P1 |
| `#/learn` | 首屏文字約 357 字，較像路線連結頁。 | 加一個完整 worked example、每階段時間、先備知識、完成判準及小測。 | P2 |
| `#/toolbox` | 只有工具入口，未展示輸出結果。 | 每個工具加「適合甚麼問題、輸入甚麼、會得到甚麼、不能回答甚麼」及實際預覽。 | P2 |
| `#/indicators` | 研究狀態面板先於指標內容；82 個結果閱讀負擔高。 | 預設顯示核心 20；按用途分段；研究狀態移頁尾；顯示篩選後分組而非長列表。 | P1 |
| 指標卡 | 摘要短而相似，只說用途。 | 加「解決問題」「何時不用」「所需資料」三個短欄；避免每卡塞公式。 | P2 |
| 指標詳情首屏 | 先顯示圖，但新手未必知道怎樣讀。 | 圖上方加「先看 1、再看 2、失效 3」；圖下加文字摘要及資料來源。 | P1 |
| 指標詳情圖表 | 82 指標共用 4 個案例，部分資料層次錯。 | 核心 20 各自有專屬案例；breadth／options 使用相應資料；其餘明示為價格背景。 | P0 |
| 指標公式摺疊 | 公式及錯誤預設關閉；13 個進階公式只是描述。 | 核心定義預設開啟；公式按 exact/algorithm 分類；補變數、平滑、warm-up、例算。 | P0 |
| 指標研究狀態 | materialCount 與比較列矛盾；`0` 意義不清。 | 分開「庫內材料」與「已接入引用」；加來源列表；修正 builder join。 | P0 |
| `#/candlesticks` | SVG 相關度高，但 pattern 深度及可靠性未量化。 | 加真實 OHLC 數字、位置／趨勢／成交量條件；不要把單一形態寫成高概率結論。 | P1 |
| `#/compare` | 13 個表格、固定比較多，初學者難找重點。 | 先問兩個指標回答同一問題嗎；輸出主指標／確認指標／重複程度／何時不用；支援任選兩個。 | P1 |
| `#/playground` | 控制和圖表資訊多；手機圖表字小。 | 控制分成「市況、指標、風險」三步；加入答案 reveal、放大圖、保存練習及錯誤回饋。 | P1 |
| `#/casebook` | 案例數和市場情境仍少；圖例重用。 | 加港股財報／配股／停牌後、美股 earnings gap、低流動性假突破；每例有前後對照。 | P2 |
| `#/glossary` | 46 個 H2，沒有字母／分類捷徑；插圖是隨機字母卡。 | 加分類索引、相關詞、例句、常見誤解；換成術語關係圖或真實詞卡。 | P2 |
| `#/subscribe` | route 名叫 subscribe，但實際是本地資料，可能令人誤會有電子報。 | 導覽和 route 改「本地資料」；說明儲存位置、私隱、匯出、合併和清除。 | P1 |
| `#/journal` | 表單完整，但新手未見填寫範例；空狀態資訊弱。 | 預載一個不可保存的示範；逐欄解釋；加入驗證、私隱提示及完成後檢查。 | P2 |
| `#/combo` | 9/10 等精確分數容易製造假確定性。 | 顯示評分權重、缺失資料和信心；主輸出改成角色覆蓋／重複／盲點，不給單一結論分數。 | P1 |
| `#/tv-strategies` | 審核方向正確，但 8 分制門檻未有方法論解釋。 | 公開 rubric、每分依據、最低交易數的例外、樣本不確定性；加入真實 Strategy Report 示意。 | P1 |
| `#/strategy-cases` | 100 目標誤當主標；內部狀態語言太多。 | 改「候選庫」；顯示 0 已通過；以「已核對／待核對／不採用」取代英文狀態。 | P0 |
| 策略詳情 | 長英文 H1 手機裁切；本地模板與原作者策略容易混淆。 | 標題可任意換行；最頂顯示「本地教學重建，非原作者代碼」；分開原始證據與重建設定。 | P0 |
| 策略設定表 | `trades`、drawdown 大多空白；仍展示 PF badge。 | 缺關鍵指標時 PF 只顯示「未完成核對」；不可用高 PF badge 建立權威感。 | P0 |
| `#/script` | 首屏只有約 209 字；重用策略庫插圖。 | 加完整 Pine 架構圖：input、condition、entry、exit、risk、alert；每段有本地短碼例。 | P2 |
| `#/script-demo` | 約 198 字，欠缺真正「同訊號不同結論」對照。 | 並排顯示同一 RSI／MA 訊號在趨勢與震盪的不同結果，標出失效位。 | P2 |
| `#/trial` | 「申請試用」像真實服務，但流程、私隱和回覆安排不清。 | 若沒有正式服務，改「試用前自我檢查」；如有服務，寫明資料用途、回覆時間和條款。 | P1 |
| Footer | 有免責聲明，但欠作者、審稿和方法論入口。 | 加「內容方法、資料來源、最後覆核日期、版本記錄、錯誤回報」。 | P1 |
| 無效 route | 未知頂層網址靜默回首頁，沒有錯誤說明。 | 顯示 404／找不到頁面、原網址及主要返回入口；不要無聲改變頁面。 | P1 |

## 7. 插圖逐張優化方向

| 圖片／使用位置 | 現況 | 需要改成 |
|---|---|---|
| `home.png` | 香港景、電腦圖表、筆記；氣氛相關，圖表不可核對。 | 一張真實教學圖，明確標出「市況、工具、失效」三步。 |
| `indicators.png` | 卡片庫和放大鏡；有搜尋感，但分類資訊是假。 | 八大用途 taxonomy 圖：趨勢、動能、成交量、波動、結構、寬度、風險、綜合。 |
| `indicator-detail.png` | 假圖表和清單；被多頁重用。 | 取消共用；核心指標使用真正專屬圖，標明公式輸入和訊號。 |
| `compare.png` | 兩張圖及勾／叉，相關度高。 | 同一標的、同一期間，左主指標、右確認指標，直接展示重複與互補。 |
| `playground.png` | 手調旋鈕和假圖表，像實驗室。 | 使用網站真實控制介面的截圖式示意，標明改一個參數會影響甚麼。 |
| `glossary.png` | 隨機英文字母索引，與金融術語弱相關。 | 用「價格→趨勢→動能→風險→執行」詞彙關係圖。 |
| `tv-strategies.png` | 程式、圖表、報告；三個頁面重用。 | 分拆三張：回測報告 anatomy、策略案例 evidence sheet、Pine code anatomy。 |
| `journal.png` | 筆記簿與圖表，相關度好。 | 顯示一份完成的交易日誌，放大理由、失效、R 值、執行四欄。 |
| `combo.png` | 多個圖示方塊，概念相關但像玩具。 | 角色矩陣：主趨勢、觸發、成交量確認、波動／止蝕、價格結構。 |
| `subscribe.png` | 如只用作本地資料，需避免「訂閱」意象。 | 本地瀏覽器→匯出 JSON→重新匯入的私隱流程圖。 |
| 陰陽燭 SVG | 現時最有教學價值。 | 保留風格；加價位刻度、真實 OHLC 數字、位置／成交量例子和手機放大。 |

## 8. 建議直接改寫的文案

| 現有文案 | 建議版本 | 原因 |
|---|---|---|
| `raw leads 已入隊 / 目標 200` | `已收集 200 個候選策略` | 移除內部流程語言。 |
| `100 個案例先過審才展示` | `策略候選庫：暫未有案例完成全部審核` | 與 accepted=0 一致。 |
| `先把數字降回證據` | `先核對數據來源和回測設定` | 直接、自然、可行動。 |
| `Runner-up 缺口 0；單一專家與共同核心情境已用 explicit status 標示。` | `每個研究概念已標明主要專家及補充角色。` | 香港讀者不需要內部欄位名稱。 |
| `momentumBreakout 保留未使用，待接線或移除` | 公開頁刪除；只在內部驗證報告保留。 | 不屬於用家需要的內容。 |
| `這個頁面已讀取本地生成資料包……` | `資料已於 2026-07-03 更新；未完成來源核對的內容會標示「待審」。` | 由系統狀態改成讀者關心的承諾。 |
| `高 PF，不代表可以實盤` | `PF 只反映指定回測；先核對交易次數、回撤、成本和測試期。` | 保留警示，同時解釋下一步。 |
| `不構成投資或交易建議` 重複出現 | 首次完整寫；其後改短標籤 `歷史回測，只作研究`。 | 減少機械重複。 |
| `量能` | 初次寫 `成交量表現（量能）`，其後用 `成交量`。 | 更貼近香港初學者。 |
| `OOS` | `樣本外測試（OOS）` | 首次出現先解釋。 |

## 9. 專業資料結構建議

### 指標資料

新增或正式落實：

`kind`、`formulaType`、`exactFormula`、`variables`、`smoothingMethod`、`warmupBars`、`requiresData`、`platformVariants`、`validRegimes`、`failureConditions`、`signalLag`、`workedExample`、`sourceRefs`、`reviewedAt`。

### 市場案例

新增：

`provenance`、`isSynthetic`、`adjustmentPolicy`、`corporateActions`、`timezone`、`session`、`downloadedAt`、`verifiedAt`、`casePurpose`、`supportedIndicatorClaims`、`unsupportedClaims`。

### TradingView 策略

正式 accepted gate 要求：

`symbol`、`exchange`、`timeframe`、`timezone`、`session`、`chartType`、`backtestStart/End`、`tradeCount`、`grossProfit/Loss`、`profitFactor`、`maxDrawdown`、`commission`、`slippage`、`fillAssumptions`、`barMagnifier`、`pyramiding`、`margin`、`OOS/walkForward`、`parameterSearchBudget`、`sourceVisibility`、`license`、`codeProvenance`。

TradingView 官方文件說明，Strategy Tester 使用 broker emulator，預設會對歷史 bar 的盤中路徑作假設；commission、slippage、limit fill verification、Bar Magnifier、calc-on-every-tick 和非標準圖表都會改變回測可信度。因此網站不應只用 PF 和勝率作主要排序。

## 10. 建議執行次序

### P0：可信度與誤導風險

1. 修正 100 案例主標與 accepted=0 狀態。
2. 修正模擬圖被標成真實 Yahoo 資料的 fallback。
3. 移除市場寬度／情緒指標的單股案例假配對。
4. 修正材料數矛盾。
5. 為進階指標補可重現公式；未完成前標示「概念算法」。

### P1：初學者與手機核心體驗

1. 手機導覽改 menu／更多。
2. 修正長英文標題、段落、表格和圖表裁切。
3. 首頁和指標庫移走內部資料狀態。
4. 核心 20 指標加入專屬案例及手機圖表摘要。
5. 表格 caption/scope、圖表文字替代、章節導覽。

### P2：內容深度與頁面差異化

1. 依八段教材結構重寫核心指標。
2. Learn、Toolbox、Glossary、Script Demo 加 worked example 和小測。
3. 分拆重用的 hero 圖，移除圖內假文字／假數據。
4. 指標組合和回測評分公開權重及限制。

### P3：資料與審稿流程

1. 補 194 筆缺年份及 113 筆 book 類 ISBN-13。
2. 建立 claim-level citation、內容版本、作者／審稿人、覆核日期。
3. close／adjClose、拆股、派息、時區和企業行動政策。
4. 每次 build 檢查來源數、公式類型、圖表 provenance 及策略 accepted gate。

### P4：設計精修

1. 減少重複 page-band 卡片感。
2. 修正首頁留白及 hero 圖載入。
3. 建立教學插圖規範：真實資料、可讀標籤、手機版本、alt／text summary。
4. 統一香港書面語和教練口語的使用位置。

## 11. 完成驗收標準

- 公開頁不會把目標數量寫成現有完成數量。
- 模擬資料與真實資料標籤永不混用。
- 82 個指標都有正確 `formulaType`；25 個進階指標可按網站資料重算。
- 市場寬度、期權情緒等指標使用相符資料源。
- 核心 20 指標各有專屬真實案例、失效例、計算例和 claim-level citations。
- 每個策略 accepted case 都有交易次數、回撤、成本、滑價、測試期、OOS 及代碼來源。
- 375px 手機寬度下，nav、H1、段落、狀態標籤及表格無不可見裁切。
- 手機圖表可以放大，並有不依賴圖像的文字摘要。
- 所有資料表有 caption 和 scope；複雜圖有詳細文字解說。
- 公開文案不再出現未解釋的 `raw leads`、`support-only`、`runner-up`、`explicit status`、`taxonomy`。
- 插圖不包含不可核對的假數據或生成式亂碼；文字標籤由 HTML/SVG 呈現。
- 靜態版和 Next.js 版只保留一個內容來源，避免兩套文案繼續漂移。

## 12. 應保留的優點

- 保留「先判斷市況，再選指標」的主線。
- 保留止蝕、倉位、R 值、成本、滑價及失效位教育。
- 保留 teal／amber／red 的研究、警告、風險語意。
- 保留本地資料、私隱和不對外傳送的設計方向。
- 保留陰陽燭自製 SVG 和真實歷史案例圖的做法，但提高資料配對及手機可讀性。
- 保留對高 PF、勝率及單一訊號的懷疑態度；這是網站最有價值的專業立場。

## 13. 參考標準

- TradingView Profit Factor 定義：`https://www.tradingview.com/support/solutions/43000681698-profit-factor/`
- TradingView RSI 計算及 Wilder RMA 示例：`https://www.tradingview.com/support/solutions/43000502338-relative-strength-index-rsi/`
- TradingView Pine Strategies／Broker Emulator：`https://www.tradingview.com/pine-script-docs/concepts/strategies/`
- TradingView Strategy Report 欄位：`https://www.tradingview.com/support/solutions/43000764138-tradingview-strategy-report-how-to-start/`
- 本地結構驗證：`data/site/validation_report.md`
- 本地策略驗證：`data/site/tradingview_strategy_validation_report.md`
